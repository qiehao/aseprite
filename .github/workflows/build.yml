name: build

on: [push, pull_request]

jobs:

  build:

    runs-on: ${{ matrix.os }}

    strategy:

      fail-fast: false

      matrix:

        os: [windows-latest]

        build_type: [Release]

        enable_ui: [on]

        include:

          - os: windows-latest

            build_type: Release

            enable_ui: on

    steps:

    - uses: actions/checkout@v3

      with:

        submodules: 'recursive'

    - name: ccache

      uses: hendrikmuhs/ccache-action@v1

      if: runner.os == 'Linux'

      with:

        key: ${{ matrix.os }}-${{ matrix.enable_ui }}

    - uses: seanmiddleditch/gha-setup-ninja@master

    - uses: ilammy/msvc-dev-cmd@v1

      if: runner.os == 'Windows'

    - name: Install Dependencies

      shell: bash

      run: |

        if [[ "${{ runner.os }}" == "Linux" ]] ; then

          sudo apt-get update -qq

          sudo apt-get install -y \

            libx11-dev libxcursor-dev libxi-dev

fi（菲涅耳）

 -名称 :生成生成文件

壳 :猛击

运行 : |

if [[ "${{ runner.os }}" == "Windows" ]] ; then

export enable_ccache=off

其他的

export enable_ccache=on

fi（菲涅耳）

卷曲-Lhttps://github.com/blueloveTH/aseprite/releases/download/v0.01/skia.zip--输出skia.zip

 7z x滑雪板拉链

cmake-S型-B型-G忍者\

-DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \

-DENABLE_UI=${{ matrix.enable_ui }} \

-DENABLE_CCACHE=$enable_ccache \

-DLAF_BACKEND=skia \

-DSKIA_DIR=./skia \

-DSKIA_LIBRARY_DIR=./skia/out/Release-x64 \

-DSKIA_LIBRARY=./skia/out/Release-x64/skia.lib

 -名称 :正在编译

壳 :猛击

运行 : |

cd构建和忍者

 -使用 :行动/上传artifact@v3

具有 :

名称 : 插入

路径 : |

D： /a/asprite/asprite/build/bin/data

D： /a/asprite/asprite/build/bin/aseprite.exe

 Author: Independent Game Grocery https://www.bilibili.com/read/cv27083106/ Origin: bilibili

